<nav class="bottom-nav">
    <a href="/" class="bottom-nav-item @(ActiveSection == "home" ? "is-active" : "")">
        <div class="bottom-nav-icon"><i class="bi bi-house"></i></div>
        <span class="bottom-nav-label">خانه</span>
        @if (ActiveSection == "home") { <span class="bottom-nav-dot"></span> }
    </a>
    <a href="/tasks/daily" class="bottom-nav-item @(ActiveSection == "tasks" ? "is-active" : "")">
        <div class="bottom-nav-icon"><i class="bi bi-check2-square"></i></div>
        <span class="bottom-nav-label">تسک‌ها</span>
        @if (ActiveSection == "tasks") { <span class="bottom-nav-dot"></span> }
    </a>
    <a href="/reports/daily" class="bottom-nav-item @(ActiveSection == "reports" ? "is-active" : "")">
        <div class="bottom-nav-icon"><i class="bi bi-bar-chart-line"></i></div>
        <span class="bottom-nav-label">گزارش‌ها</span>
        @if (ActiveSection == "reports") { <span class="bottom-nav-dot"></span> }
    </a>
    <a href="/notes" class="bottom-nav-item @(ActiveSection == "notes" ? "is-active" : "")">
        <div class="bottom-nav-icon"><i class="bi bi-journal-text"></i></div>
        <span class="bottom-nav-label">یادداشت‌ها</span>
        @if (ActiveSection == "notes") { <span class="bottom-nav-dot"></span> }
    </a>
    <a href="/goals" class="bottom-nav-item @(ActiveSection == "goals" ? "is-active" : "")">
        <div class="bottom-nav-icon"><i class="bi bi-bullseye"></i></div>
        <span class="bottom-nav-label">اهداف</span>
        @if (ActiveSection == "goals") { <span class="bottom-nav-dot"></span> }
    </a>
</nav>

@code {
    [Parameter] public string ActiveSection { get; set; } = "";
}